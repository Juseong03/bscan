torch>=2.0.0
torchvision
numpy
pandas
scipy
scikit-learn
matplotlib
seaborn
einops          # used by mamba / transformer model variants

# RNA Foundation Models — pinned to a verified-working combination.
# (multimolecule/transformers/tokenizers are version-sensitive; mismatches cause
#  tokenizer/loader errors. These exact versions are known to work together.)
multimolecule==0.0.7
transformers==4.46.0
tokenizers==0.20.3
huggingface-hub==0.32.5
safetensors==0.5.3
protobuf          # required by RnaTokenizer (FM tokenizers)
sentencepiece     # required by RnaTokenizer (FM tokenizers)
tqdm

# Thermodynamic analysis
ViennaRNA>=2.6.0

# Cross-dataset leakage control (analysis/make_external_b_hostgene.py)
pyliftover

# Optional: Mamba SSM
mamba-ssm>=1.2.0
causal-conv1d>=1.2.0
